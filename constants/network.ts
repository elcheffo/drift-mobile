import { DriftEnv } from "@drift-labs/sdk/lib/config";
import { clusterApiUrl } from "@solana/web3.js";

export const DRIFT_NETWORK: DriftEnv = 'devnet';

export const RPC_URL = clusterApiUrl("devnet");